# Chave da API da Anthropic -- nunca comite a chave real, nunca a exponha no frontend.
ANTHROPIC_API_KEY=sk-ant-...

# Opcional -- porta do servidor (default 3000).
# PORT=3000

# Opcional -- modelo usado nas duas etapas de IA (default claude-sonnet-5).
# ANTHROPIC_MODEL=claude-sonnet-5
